# Claude Code handoff — EBDESIGN integrated completion work

**Prepared:** 15 September 2026. **User target:** make progress before Codex returns on 19 September. **Branch:** `codex/production-reconcile-auth-geo`. This is a work request through the repository's shared `.ai/` handoff protocol; it is not a claim that a live Claude session received or accepted it.

## User direction and design authority

Build the EBDESIGN/Lumo Earth system as integrated business projects, with all frontend pages, backend modules and skeletons brought to actual workflow completion. National marketplace scope is **all India** in both directions: North-East farmers and other-state producers sell nationwide; farmers and verified family buyers can buy household goods and farm equipment/inputs from any state. North-East corridors have early local validation priority, not exclusive market scope. Basic-phone IVR/SMS, web/PWA, Android and desktop share one account, permission and transaction state. Preserve the two AI backbones (library-grounded external models; governed internal ERP action agents), seven AI capability classes, cost/revenue/production levers, enterprise ERP, GST/accounting, insurance, subsidies, food/health/agronomy, engineering, village freelancers/CSR, shared assets and second-life equipment. Later user corrections outrank stale Northeast-only prose in `CLAUDE.md`.

Read `docs/PROJECT_CONCEPT_RECONCILIATION_20260915.md`, `docs/PRODUCTION_COMPLETION_TODO.md`, `docs/PRODUCT_ECOSYSTEM_BLUEPRINT.md`, `docs/FARMER_PROCUREMENT_BUSINESS_MODEL.md`, `_EBDESIGN_LIBRARY/04_AUTHORITY`, and `.ai/AGENT_PROTOCOL.md` before implementation. The library is a forensic discovery aid; module presence is not production readiness. Do module-specific duplicate cleanup only after hash/symbol/caller/mount/function comparison and regression verification. Preserve unique variant behavior.

## Committed baseline and evidence

- `f9f048c8`: canonical auth/geofence, AI gateway, accounting/GST and advanced AI foundations.
- `981675bb`: seven-class rural AI bindings and secured voice workflow.
- `9db28808`: 28-state/8-UT market registry, nationally selectable seller origins and secured listing routes.
- `36e52e7c`: persisted supplier draft/independent approval; farmer/family quote comparison by approved effective terms, served destination, MOQ, stock, actual quoted bulk tier and provisional landed cost; append-only event. Three focused Jest tests/lint pass.
- `bd4a8e58`: actor-scoped household plan/subscription reads, real plan/group details, head-of-household creation authority, responsive household-to-procurement pages and route. Two focused backend tests/lint and frontend build pass.

No live PostgreSQL transcript exists for the new migrations. AI Backbone live generation previously returned `credit_balance_exhausted`. Quantum provider/robot hardware are unavailable. The full repository readiness audit on 15 September failed: 390 listed broken imports and 1,626 placeholder detections. Its regex scans comments and literal/generated imports, so classify findings before deletion or readiness claims. The frontend build succeeded; source file counts are not acceptance evidence.

Unrelated local modifications `.audit/production-readiness.json`, `.vibecheck/*`, `.claude/settings.local.json` belong to the workspace/user; do not stage, reset or delete them. A small addendum in `docs/FARMER_PROCUREMENT_BUSINESS_MODEL.md` may be staged separately; check `git status` first. No confirmed remote push/upstream.

## Work Claude can independently complete before 19 September

1. **Commerce order-contract reconciliation:** trace `backend/src/services/commerce/orderService.js`, `backend/src/database/migrations/042_rural_procurement_logistics_mobility_schema.sql`, household planning, quote runs, buying clubs, GST/accounting, payments, return-load/fulfillment and current checkout pages. The existing `procurement_orders` has no authenticated buyer/family authority or quote approval lineage. Propose and implement one canonical transaction boundary with immutable actor/quote/tax/freight snapshots, stock/club commitment and idempotency; only expose checkout once price, tax, subsidy, carrier and payment consent are confirmed. Never count provisional quote savings as realized savings. Add negative authorization, stale-offer, quantity/stock and accounting reconciliation tests.
2. **Frontend API contract repair within that same journey:** verify every method/path on HouseholdProcurementPage, FarmerProcurementPage, checkout/cart, farm-input and buying-club surfaces against mounted routes and response shapes. Fix actual mismatches with shared `api` clients and account permissions; do not introduce generic mock success or fixed example values. Test desktop and 320px/mobile navigation, loading/error/empty states and server-confirmed outcomes.
3. **Audit classification and module cleanup:** start with `tools/production-readiness-audit.js` and `.audit/production-readiness.json`; classify static runtime broken imports separately from comment/generated false positives and unreachable historical variants. Repair the first live high-risk import chains and submit an evidence table. The 300-plus numbered `frontend/src/modules/M031...` pages have a literal `import './${className}.css'` and generic `/api/m031` pattern; prove whether the `ModuleRuntimePage` is canonical and these files are reachable before removing or consolidating them. Do not mass-delete from a regex result.
4. **Next integrated vertical lane if time remains:** choose institutional village artisan/CSR commerce or shared capacity/second-life machinery with full actor→RFQ/reservation→price/cost→quality/custody→invoice/payment/ERP→outcome path. Reuse current partial services/schema instead of parallel modules. Declare unconfigured external provider states honestly.

For each finished lane, provide canonical file and duplicate disposition, schema/migration execution status, mounted authenticated API, responsive operator/buyer workflow, immutable evidence, tests, failure/degraded state and remaining integration gates. Keep partial foundation labeled partial. Do not claim the entire project is complete from a green unit test or presence scan. Leave a concise result/update in `.ai/handoffs/` and `.ai/tasks/ACTIVE.md` for Codex to inspect on 19 September.
