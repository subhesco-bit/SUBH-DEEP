# API-022: GET /api/v1/ai/copilot/history

**Phase:** 4.2 (Major Routes)  
**Service:** aiCopilotService  
**Priority:** P1 - High  
**Status:** Specification Complete  

## Endpoint Specification

### Method & Path
```
GET /api/v1/ai/copilot/history
```

### Purpose
Retrieve chat history for the AI copilot. Allows users to review previous conversations and continue ongoing discussions.

### Service Dependency
- `backend/src/services/legacy/aiCopilotService.js`
- Method: `getChatHistory(userId, filters)`

## Request Specification

### Headers
```
Authorization: Bearer {access_token}
Content-Type: application/json
```

### Query Parameters
```json
{
  "session_id": "string (optional)",
  "limit": "number (optional, default: 20)",
  "offset": "number (optional, default: 0)",
  "date_from": "ISO8601 datetime (optional)",
  "date_to": "ISO8601 datetime (optional)"
}
```

### Validation Rules
- User must be authenticated
- User can only view their own chat history
- Pagination parameters must be valid
- Date range must be valid if provided

## Response Specification

### Success Response (200 OK)
```json
{
  "success": true,
  "data": {
    "sessions": [
      {
        "session_id": "string",
        "created_at": "ISO8601 datetime",
        "updated_at": "ISO8601 datetime",
        "message_count": "number",
        "module": "string",
        "summary": "string"
      }
    ],
    "messages": [
      {
        "message_id": "uuid",
        "session_id": "string",
        "role": "string (user/assistant)",
        "content": "string",
        "timestamp": "ISO8601 datetime",
        "context": {}
      }
    ],
    "pagination": {
      "limit": "number",
      "offset": "number",
      "total": "number"
    }
  }
}
```

### Error Responses

**400 Bad Request**
```json
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Invalid query parameters"
  }
}
```

**401 Unauthorized**
```json
{
  "success": false,
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Authentication required"
  }
}
```

## Authentication Requirements
- Valid access token required
- Rate limiting: 60 requests per minute per user

## Security Considerations
- Ensure users can only access their own chat history
- Implement data retention policies
- Consider implementing message redaction for sensitive content
- Rate limit to prevent data scraping

## Implementation Checklist
- [ ] Create handler function in `backend/src/routes/aiCopilotRoutes.js`
- [ ] Add authentication middleware
- [ ] Add input validation middleware
- [ ] Add rate limiting middleware
- [ ] Implement error handling with proper status codes
- [ ] Write integration test in `backend/src/routes/__tests__/aiCopilot.test.js`
- [ ] Mount in `backend/src/index.js` via `app.use('/api/v1/ai/copilot', aiCopilotRoutes)`
- [ ] Test with Postman/curl against running server
- [ ] Document in API spec with cURL examples
- [ ] Verify request/response matches specification

## Example cURL Commands

### Get chat history
```bash
curl -X GET "http://localhost:3000/api/v1/ai/copilot/history?limit=10&offset=0" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -H "Content-Type: application/json"
```

### Get specific session history
```bash
curl -X GET "http://localhost:3000/api/v1/ai/copilot/history?session_id=session_123" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -H "Content-Type: application/json"
```

### Get history with date range
```bash
curl -X GET "http://localhost:3000/api/v1/ai/copilot/history?date_from=2024-08-01T00:00:00Z&date_to=2024-08-31T23:59:59Z" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -H "Content-Type: application/json"
```

## Dependencies
- AI copilot service
- Chat history storage
- User authentication system
- Pagination system

## Notes
- Implement session-based grouping
- Provide session summaries for easy navigation
- Support pagination for large histories
- Consider implementing search within chat history
- Handle message deletion requests
- Implement data retention policies

---

*Generated for Phase 4.2 Major Routes Implementation*