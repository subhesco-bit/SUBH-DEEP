# API-045: GET /api/v1/library/card/:id

**Phase:** 4.2 (Major Routes)  
**Service:** libraryKnowledgeService  
**Priority:** P1 - High  
**Status:** Specification Complete  

## Endpoint Specification

### Method & Path
```
GET /api/v1/library/card/:id
```

### Purpose
Retrieve detailed content from a specific library card. Provides complete access to module documentation and knowledge resources.

### Service Dependency
- `backend/src/services/claude/libraryKnowledgeService.js`
- Method: `getLibraryCard(cardId)`

## Request Specification

### Headers
```
Authorization: Bearer {access_token}
Content-Type: application/json
```

### URL Parameters
- `id` (string, required): Library card ID

### Query Parameters
```json
{
  "include_related": "boolean (optional, default: true)",
  "language": "string (optional, default: en)"
}
```

### Validation Rules
- User must be authenticated
- Card must exist in library
- Language must be supported if provided

## Response Specification

### Success Response (200 OK)
```json
{
  "success": true,
  "data": {
    "card": {
      "id": "string",
      "title": "string",
      "category": "string",
      "module_id": "uuid",
      "module_name": "string",
      "content_type": "string",
      "content": "string",
      "summary": "string",
      "key_points": ["array"],
      "tags": ["array"],
      "author": "string",
      "version": "string",
      "last_updated": "ISO8601 datetime",
      "language": "string"
    },
    "related_cards": [
      {
        "id": "string",
        "title": "string",
        "category": "string",
        "relevance": "string"
      }
    ],
    "metadata": {
      "views": "number",
      "useful_count": "number",
      "content_hash": "string"
    }
  }
}
```

### Error Responses

**401 Unauthorized**
```json
{
  "success": false,
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Authentication required"
  }
}
```

**404 Not Found**
```json
{
  "success": false,
  "error": {
    "code": "CARD_NOT_FOUND",
    "message": "Library card not found"
  }
}
```

## Authentication Requirements
- Valid access token required
- Rate limiting: 60 requests per minute per user

## Security Considerations
- Validate card access permissions
- Implement content filtering based on user permissions
- Rate limit to prevent abuse
- Log card access for analytics
- Cache frequently accessed cards

## Implementation Checklist
- [ ] Create handler function in `backend/src/routes/libraryRoutes.js`
- [ ] Add authentication middleware
- [ ] Add input validation middleware
- [ ] Add rate limiting middleware
- [ ] Implement error handling with proper status codes
- [ ] Write integration test in `backend/src/routes/__tests__/library.test.js`
- [ ] Mount in `backend/src/index.js` via `app.use('/api/v1/library', libraryRoutes)`
- [ ] Test with Postman/curl against running server
- [ ] Document in API spec with cURL examples
- [ ] Verify request/response matches specification

## Example cURL Commands

### Get library card content
```bash
curl -X GET "http://localhost:3000/api/v1/library/card/M001_GUIDE_001?include_related=true" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -H "Content-Type: application/json"
```

### Get card in specific language
```bash
curl -X GET "http://localhost:3000/api/v1/library/card/M001_GUIDE_001?language=hi" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -H "Content-Type: application/json"
```

## Dependencies
- Library knowledge service
- Library catalog system
- Content management system
- User authentication system
- Caching layer

## Notes
- Provide complete card content with formatting
- Include related cards for context
- Track card views and usefulness
- Support multiple languages
- Implement content versioning
- Provide card download/export functionality
- Consider implementing card editing for authorized users

---

*Generated for Phase 4.2 Major Routes Implementation*