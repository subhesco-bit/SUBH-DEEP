# API-046: POST /api/v1/search/advanced

**Phase:** 4.3 (Optional Routes)  
**Service:** advancedSearchService  
**Priority:** P2 - Medium  
**Status:** Specification Complete  

## Endpoint Specification

### Method & Path
```
POST /api/v1/search/advanced
```

### Purpose
Perform advanced search across the platform with complex filters, faceted search, and custom ranking. Provides powerful search capabilities for power users.

### Service Dependency
- `backend/src/services/advancedSearchService.js`
- Method: `advancedSearch(searchQuery, filters)`

## Request Specification

### Headers
```
Authorization: Bearer {access_token}
Content-Type: application/json
```

### Body Schema
```json
{
  "query": "string (required)",
  "search_scope": "string (required, enum: all, products, orders, farmers, knowledge, marketplace)",
  "filters": {
    "date_range": {
      "start_date": "ISO8601 datetime (optional)",
      "end_date": "ISO8601 datetime (optional)"
    },
    "price_range": {
      "min": "number (optional)",
      "max": "number (optional)"
    },
    "location": {
      "radius_km": "number (optional)",
      "coordinates": {
        "lat": "number (optional)",
        "lng": "number (optional)"
      }
    },
    "categories": ["array of strings (optional)"],
    "tags": ["array of strings (optional)"],
    "status": ["array of strings (optional)"],
    "custom_fields": "object (optional)"
  },
  "sorting": {
    "field": "string (optional)",
    "order": "string (optional, enum: asc, desc, default: desc)"
  },
  "pagination": {
    "page": "number (optional, default: 1)",
    "limit": "number (optional, default: 20, max: 100)"
  },
  "search_options": {
    "fuzzy_match": "boolean (optional, default: true)",
    "include_synonyms": "boolean (optional, default: false)",
    "boost_recent": "boolean (optional, default: true)"
  }
}
```

### Validation Rules
- User must be authenticated
- Query must not be empty
- Search scope must be valid
- Pagination parameters must be valid

## Response Specification

### Success Response (200 OK)
```json
{
  "success": true,
  "data": {
    "query": "string",
    "search_scope": "string",
    "results": [
      {
        "id": "uuid",
        "type": "string",
        "title": "string",
        "summary": "string",
        "relevance_score": "number",
        "highlighted_fields": {},
        "metadata": {}
      }
    ],
    "facets": {
      "categories": [
        {
          "value": "string",
          "count": "number"
        }
      ],
      "price_ranges": [
        {
          "range": "string",
          "count": "number"
        }
      ]
    },
    "pagination": {
      "page": "number",
      "limit": "number",
      "total": "number",
      "total_pages": "number"
    },
    "search_time_ms": "number"
  }
}
```

### Error Responses

**400 Bad Request**
```json
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Invalid advanced search parameters"
  }
}
```

**401 Unauthorized**
```json
{
  "success": false,
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Authentication required"
  }
}
```

## Authentication Requirements
- Valid access token required
- Rate limiting: 30 requests per minute per user

## Security Considerations
- Implement query sanitization
- Rate limit to prevent abuse
- Log search queries for analytics
- Implement result filtering based on user permissions

## Implementation Checklist
- [ ] Create handler function in `backend/src/routes/advancedSearchRoutes.js`
- [ ] Add authentication middleware
- [ ] Add input validation middleware
- [ ] Add rate limiting middleware
- [ ] Implement error handling with proper status codes
- [ ] Write integration test in `backend/src/routes/__tests__/advancedSearch.test.js`
- [ ] Mount in `backend/src/index.js` via `app.use('/api/v1/search', advancedSearchRoutes)`
- [ ] Test with Postman/curl against running server
- [ ] Document in API spec with cURL examples
- [ ] Verify request/response matches specification

## Example cURL Commands

### Advanced search with filters
```bash
curl -X POST http://localhost:3000/api/v1/search/advanced \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -H "Content-Type: application/json" \
  -d '{
    "query": "organic rice",
    "search_scope": "products",
    "filters": {
      "price_range": {
        "min": 10,
        "max": 50
      },
      "categories": ["grains"],
      "tags": ["organic", "premium"]
    },
    "sorting": {
      "field": "price",
      "order": "asc"
    },
    "pagination": {
      "page": 1,
      "limit": 20
    }
  }'
```

## Dependencies
- Advanced search service
- Search indexing service
- Faceted search engine
- User authentication system
- Caching layer

## Notes
- Implement faceted search for filtering
- Provide search result highlighting
- Support custom field search
- Implement search suggestions
- Cache popular search queries
- Provide search analytics
- Consider implementing search history

---

*Generated for Phase 4.3 Optional Routes Implementation*