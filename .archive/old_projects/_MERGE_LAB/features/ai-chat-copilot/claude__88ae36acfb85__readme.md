# M082 - Weather Forecasting (frontend)

Domain: Climate
Status: BUILT — implemented at `frontend/src/pages/ClimateWeatherPage.jsx`

This module's UI is not built here. Forecast provider accuracy (measured
against observations, by provider and horizon) is implemented in the
"Forecast provider accuracy" section of `ClimateWeatherPage.jsx`, mounted
at `/climate` (migration 057, D14, modules M081-M090). This stub
(`M082Page.jsx`) redirects to it rather than duplicating the implementation.
