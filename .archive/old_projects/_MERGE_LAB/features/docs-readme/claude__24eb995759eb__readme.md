# M022 - Farmer Profile (frontend)

Domain: Farmer
Status: BUILT — implemented at `frontend/src/pages/FarmerProfilePage.jsx`

This module's UI is not built here. Farmer Profile is implemented in `FarmerProfilePage.jsx`, mounted at `/farmer-profile`. This stub (`M022Page.jsx`) redirects to it rather than duplicating the implementation.
