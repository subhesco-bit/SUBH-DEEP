# M002 - Platform Configuration (frontend)

Domain: Platform Foundation
Status: HIDDEN — implemented at `frontend/src/pages/AdminDashboardPage.jsx`

This module's UI is not built here. Platform configuration (feature flags,
system parameters, integration settings) lives in the Settings tab of
`AdminDashboardPage.jsx`, mounted at `/admin-dashboard`. This stub
(`M002Page.jsx`) redirects to it rather than duplicating the implementation.
