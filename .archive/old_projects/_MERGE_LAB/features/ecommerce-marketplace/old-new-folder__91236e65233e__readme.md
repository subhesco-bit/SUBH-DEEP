# M053 - FPO Membership (frontend)

Domain: FPO
Status: HIDDEN — implemented at `frontend/src/pages/FPODashboardPage.jsx` ("Members" tab)

This module's UI is not built here. FPO membership management is implemented
in the "Members" tab of `FPODashboardPage.jsx`, mounted at `/fpo-dashboard`.
This stub (`M053Page.jsx`) redirects to it rather than duplicating the
implementation.
