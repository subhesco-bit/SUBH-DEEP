// Express routes for Pest Forecasting (M087)
const express = require('express');
const router = express.Router();
// const controller = require('./controller');

// Example: router.get('/', controller.list);
module.exports = router;
