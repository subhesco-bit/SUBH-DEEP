node_modules/
dist/
build/
coverage/
frontend/src-tauri/target/
*.log
npm-debug.log*
.env
.env.local
.env.*.local
.DS_Store
Thumbs.db
*.tsbuildinfo
.vscode/
.idea/
.vs/
*.sqlite
*.sqlite3
.vibecheck/provenance/attestations.db
auth_store.json
backend/src/database/*.json

# Unrelated local AI-model download cache (~17GB Ollama-format blob,
# interrupted mid-pull) - not part of this project, see blobs/_WHAT_IS_THIS.md.
# Ignored, not deleted, so it never gets accidentally committed.
blobs/

# Pre-existing local audit/catalog data, never committed before, too large
# for git regardless of value (2026-08-29: largest single CSV here is 204MB,
# over GitHub's 100MB hard file-size limit - a push including it fails
# outright). Real infrastructure, not junk - just doesn't belong in git.
_EBDESIGN_LIBRARY/
_SQL_INFRA/
# One-off local filesystem inventory dumps generated for ad-hoc audits
# (Desktop tools, PowerShell scripts) - regenerable, not source.
FileAuditReport.csv
FileAuditIndex.csv
