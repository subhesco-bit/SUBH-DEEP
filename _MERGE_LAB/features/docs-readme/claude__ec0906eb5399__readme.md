# M015 - Permission Management (frontend)

Domain: Identity
Status: BUILT — implemented at `frontend/src/pages/IdentityManagementPage.jsx`

This module's UI is not built here. It is implemented as a tab on the
consolidated IdentityManagementPage.jsx, mounted at `/identity-management`. This stub (`M015Page.jsx`)
points to it rather than duplicating the implementation.
