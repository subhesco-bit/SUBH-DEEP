# M093 - Labour Management (frontend)

Domain: Operations
Status: BUILT — implemented at `frontend/src/pages/LabourManagementPage.jsx`

This module's UI is not built here. Worker records and attendance tracking
are implemented in `LabourManagementPage.jsx`, mounted at
`/labour-management`. This stub (`M093Page.jsx`) redirects to it rather
than duplicating the implementation.
