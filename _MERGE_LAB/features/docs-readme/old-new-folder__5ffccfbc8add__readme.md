# M111 - Seed Inventory (frontend)

Domain: Input Supply
Status: BUILT — implemented at `frontend/src/pages/SeedVaultPage.jsx`

This module's UI is not built here. It is implemented as the "Seed
Inventory Grid" in `SeedVaultPage.jsx`, mounted at `/seedvault`. This stub
(`M111Page.jsx`) redirects to it rather than duplicating the
implementation. Found by the third-batch (2026-08-08) hidden-module scan.
