# M031 - Land Registry (frontend)

Domain: Land
Status: BUILT — implemented at `frontend/src/pages/LandRegistryPage.jsx`

This module's UI is not built here. Land parcel records and title-status
tracking are implemented in `LandRegistryPage.jsx`, mounted at
`/land-registry`. This stub (`M031Page.jsx`) redirects to it rather than
duplicating the implementation.
