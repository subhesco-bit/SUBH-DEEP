# M058 - FPO Sales (frontend)

Domain: FPO
Status: HIDDEN — implemented at `frontend/src/pages/FPODashboardPage.jsx` ("Collective Orders" tab)

This module's UI is not built here. FPO sales (collective orders) is
implemented in the "Collective Orders" tab of `FPODashboardPage.jsx`,
mounted at `/fpo-dashboard`. This stub (`M058Page.jsx`) redirects to it
rather than duplicating the implementation.
