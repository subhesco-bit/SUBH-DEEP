# M013 - Authorization (frontend)

Domain: Identity
Status: BUILT — implemented at `frontend/src/pages/AuthorizationPage.jsx`

This module's UI is not built here. Role and permission management is
implemented in `AuthorizationPage.jsx`, mounted at `/authorization`. This
stub (`M013Page.jsx`) redirects to it rather than duplicating the
implementation.
