# M040 - Digital Land Records (frontend)

Domain: Land
Status: BUILT — implemented at `frontend/src/pages/LandRegistryPage.jsx`

This module's UI is not built here. Digitized land parcel and title-status
records (parcel code, owner, survey number, title status) are implemented
in `LandRegistryPage.jsx`, mounted at `/land-registry` (one of the
first-batch 15 modules). This stub (`M040Page.jsx`) redirects to it rather
than duplicating the implementation.
