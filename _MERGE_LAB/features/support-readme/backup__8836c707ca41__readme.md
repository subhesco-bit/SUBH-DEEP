# M098 - Farm Costing (frontend)

Domain: Operations
Status: BUILT — implemented at `frontend/src/pages/FarmCostingPage.jsx`

This module's UI is not built here. Per-crop cost and expected-revenue
tracking is implemented in `FarmCostingPage.jsx`, mounted at
`/farm-costing`. This stub (`M098Page.jsx`) redirects to it rather than
duplicating the implementation.
