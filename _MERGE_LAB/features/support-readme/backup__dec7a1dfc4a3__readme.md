# M106 - Preventive Maintenance (frontend)

Domain: Machinery
Status: BUILT — implemented at `frontend/src/pages/MachineryManagementPage.jsx`

This module's UI is not built here. It is implemented as a tab on the
consolidated MachineryManagementPage.jsx, mounted at `/machinery-management`. This stub (`M106Page.jsx`)
points to it rather than duplicating the implementation.
