# CI/CD workflows — not yet set up

`afrera/afrera-devops/README.md` describes CI/CD pipelines for this project
("defined using GitHub Actions", configured under `.github/workflows`), but no
actual workflow files exist yet in this directory.

The previous contents of `afrera/.github/workflows` (a plain text file, not a
valid workflow) were a leftover raw chat transcript from a prior AI session
and have been removed. This placeholder marks the intended location until
real CI workflow files are authored.
