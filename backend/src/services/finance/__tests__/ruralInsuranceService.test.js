'use strict';const {RuralInsuranceService}=require('../ruralInsuranceService');describe('rural insurance decisions',()=>{const s=new RuralInsuranceService({database:{}});test('quote preserves subsidy and evidence calculation',()=>expect(s.quote({sumInsured:100000,ratePercent:2,subsidyPercent:50})).toEqual(expect.objectContaining({basePremium:2000,subsidy:1000,farmerPayable:1000})));test('fraud scoring explains but never decides claim',()=>expect(s.fraudScore({claimAmount:120,sumInsured:100,duplicateEvidence:1})).toEqual(expect.objectContaining({band:'high',decision:'advisory_review_only'})));test('AI is advisory only',async()=>{const x=new RuralInsuranceService({database:{},gateway:{run:jest.fn(async()=>({success:true}))}});await expect(x.advise({objective:'review'},'u')).resolves.toEqual(expect.objectContaining({decisionAuthority:false,humanReviewRequired:true}))})});
