# M032 - Land Ownership (frontend)

Domain: Land
Status: BUILT — implemented at `frontend/src/pages/LandRegistryPage.jsx`

This module's UI is not built here. Land ownership records (owner_name, ownership_type, title_status per parcel) are already tracked in `LandRegistryPage.jsx`, mounted at `/land-registry` (one of the first-batch 15 modules). This stub (`M032Page.jsx`) redirects to it rather than duplicating the implementation.
