# M060 - FPO Analytics (frontend)

Domain: FPO
Status: HIDDEN — implemented at `frontend/src/pages/FPODashboardPage.jsx` ("Overview" stats tab)

This module's UI is not built here. FPO analytics is implemented in the
"Overview" stats tab of `FPODashboardPage.jsx`, mounted at `/fpo-dashboard`.
This stub (`M060Page.jsx`) redirects to it rather than duplicating the
implementation.
