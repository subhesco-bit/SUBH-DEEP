export const ApiClient113 = {
  get: async (url) => ({ data: null }),
  post: async (url, data) => ({ data: null }),
  put: async (url, data) => ({ data: null }),
  delete: async (url) => ({ data: null })
};
