import { createContext } from 'react';
export const Store014Context = createContext();
export function Store014Provider({ children }) {
  return <Store014Context.Provider value={{}}>{children}</Store014Context.Provider>;
}
