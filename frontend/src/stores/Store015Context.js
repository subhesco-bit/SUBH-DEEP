import { createContext } from 'react';
export const Store015Context = createContext();
export function Store015Provider({ children }) {
  return <Store015Context.Provider value={{}}>{children}</Store015Context.Provider>;
}
