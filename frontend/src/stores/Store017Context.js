import { createContext } from 'react';
export const Store017Context = createContext();
export function Store017Provider({ children }) {
  return <Store017Context.Provider value={{}}>{children}</Store017Context.Provider>;
}
