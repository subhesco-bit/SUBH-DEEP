import { createContext } from 'react';
export const Store019Context = createContext();
export function Store019Provider({ children }) {
  return <Store019Context.Provider value={{}}>{children}</Store019Context.Provider>;
}
