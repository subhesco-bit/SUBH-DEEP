import { createContext } from 'react';
export const Store026Context = createContext();
export function Store026Provider({ children }) {
  return <Store026Context.Provider value={{}}>{children}</Store026Context.Provider>;
}
