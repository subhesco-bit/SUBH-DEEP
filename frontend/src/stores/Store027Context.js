import { createContext } from 'react';
export const Store027Context = createContext();
export function Store027Provider({ children }) {
  return <Store027Context.Provider value={{}}>{children}</Store027Context.Provider>;
}
