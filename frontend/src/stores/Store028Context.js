import { createContext } from 'react';
export const Store028Context = createContext();
export function Store028Provider({ children }) {
  return <Store028Context.Provider value={{}}>{children}</Store028Context.Provider>;
}
