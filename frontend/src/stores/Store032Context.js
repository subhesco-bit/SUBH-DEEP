import { createContext } from 'react';
export const Store032Context = createContext();
export function Store032Provider({ children }) {
  return <Store032Context.Provider value={{}}>{children}</Store032Context.Provider>;
}
