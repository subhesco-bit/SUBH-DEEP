import { createContext } from 'react';
export const Store033Context = createContext();
export function Store033Provider({ children }) {
  return <Store033Context.Provider value={{}}>{children}</Store033Context.Provider>;
}
