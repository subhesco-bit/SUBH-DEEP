import { createContext } from 'react';
export const Store034Context = createContext();
export function Store034Provider({ children }) {
  return <Store034Context.Provider value={{}}>{children}</Store034Context.Provider>;
}
