import { createContext } from 'react';
export const Store036Context = createContext();
export function Store036Provider({ children }) {
  return <Store036Context.Provider value={{}}>{children}</Store036Context.Provider>;
}
