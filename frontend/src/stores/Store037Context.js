import { createContext } from 'react';
export const Store037Context = createContext();
export function Store037Provider({ children }) {
  return <Store037Context.Provider value={{}}>{children}</Store037Context.Provider>;
}
