import { createContext } from 'react';
export const Store038Context = createContext();
export function Store038Provider({ children }) {
  return <Store038Context.Provider value={{}}>{children}</Store038Context.Provider>;
}
