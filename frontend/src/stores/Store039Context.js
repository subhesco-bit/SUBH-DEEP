import { createContext } from 'react';
export const Store039Context = createContext();
export function Store039Provider({ children }) {
  return <Store039Context.Provider value={{}}>{children}</Store039Context.Provider>;
}
