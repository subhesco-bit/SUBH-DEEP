import { createContext } from 'react';
export const Store043Context = createContext();
export function Store043Provider({ children }) {
  return <Store043Context.Provider value={{}}>{children}</Store043Context.Provider>;
}
