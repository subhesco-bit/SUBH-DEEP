import { createContext } from 'react';
export const Store044Context = createContext();
export function Store044Provider({ children }) {
  return <Store044Context.Provider value={{}}>{children}</Store044Context.Provider>;
}
