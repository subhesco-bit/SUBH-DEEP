import { createContext } from 'react';
export const Store045Context = createContext();
export function Store045Provider({ children }) {
  return <Store045Context.Provider value={{}}>{children}</Store045Context.Provider>;
}
