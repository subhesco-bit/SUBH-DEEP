import { createContext } from 'react';
export const Store046Context = createContext();
export function Store046Provider({ children }) {
  return <Store046Context.Provider value={{}}>{children}</Store046Context.Provider>;
}
