import { createContext } from 'react';
export const Store047Context = createContext();
export function Store047Provider({ children }) {
  return <Store047Context.Provider value={{}}>{children}</Store047Context.Provider>;
}
