import { createContext } from 'react';
export const Store048Context = createContext();
export function Store048Provider({ children }) {
  return <Store048Context.Provider value={{}}>{children}</Store048Context.Provider>;
}
