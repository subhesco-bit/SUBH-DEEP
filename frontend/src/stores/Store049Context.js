import { createContext } from 'react';
export const Store049Context = createContext();
export function Store049Provider({ children }) {
  return <Store049Context.Provider value={{}}>{children}</Store049Context.Provider>;
}
