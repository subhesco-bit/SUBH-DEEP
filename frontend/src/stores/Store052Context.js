import { createContext } from 'react';
export const Store052Context = createContext();
export function Store052Provider({ children }) {
  return <Store052Context.Provider value={{}}>{children}</Store052Context.Provider>;
}
