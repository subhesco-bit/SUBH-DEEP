import { createContext } from 'react';
export const Store053Context = createContext();
export function Store053Provider({ children }) {
  return <Store053Context.Provider value={{}}>{children}</Store053Context.Provider>;
}
