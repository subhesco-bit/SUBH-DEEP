import { createContext } from 'react';
export const Store054Context = createContext();
export function Store054Provider({ children }) {
  return <Store054Context.Provider value={{}}>{children}</Store054Context.Provider>;
}
