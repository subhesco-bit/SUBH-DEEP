import { createContext } from 'react';
export const Store055Context = createContext();
export function Store055Provider({ children }) {
  return <Store055Context.Provider value={{}}>{children}</Store055Context.Provider>;
}
