import { createContext } from 'react';
export const Store056Context = createContext();
export function Store056Provider({ children }) {
  return <Store056Context.Provider value={{}}>{children}</Store056Context.Provider>;
}
