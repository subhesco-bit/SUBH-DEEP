import { createContext } from 'react';
export const Store057Context = createContext();
export function Store057Provider({ children }) {
  return <Store057Context.Provider value={{}}>{children}</Store057Context.Provider>;
}
