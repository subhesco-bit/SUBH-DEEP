import { createContext } from 'react';
export const Store058Context = createContext();
export function Store058Provider({ children }) {
  return <Store058Context.Provider value={{}}>{children}</Store058Context.Provider>;
}
