import { createContext } from 'react';
export const Store059Context = createContext();
export function Store059Provider({ children }) {
  return <Store059Context.Provider value={{}}>{children}</Store059Context.Provider>;
}
