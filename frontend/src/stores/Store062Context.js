import { createContext } from 'react';
export const Store062Context = createContext();
export function Store062Provider({ children }) {
  return <Store062Context.Provider value={{}}>{children}</Store062Context.Provider>;
}
