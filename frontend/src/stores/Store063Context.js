import { createContext } from 'react';
export const Store063Context = createContext();
export function Store063Provider({ children }) {
  return <Store063Context.Provider value={{}}>{children}</Store063Context.Provider>;
}
