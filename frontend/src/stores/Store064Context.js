import { createContext } from 'react';
export const Store064Context = createContext();
export function Store064Provider({ children }) {
  return <Store064Context.Provider value={{}}>{children}</Store064Context.Provider>;
}
