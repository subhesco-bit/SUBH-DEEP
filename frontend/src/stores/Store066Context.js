import { createContext } from 'react';
export const Store066Context = createContext();
export function Store066Provider({ children }) {
  return <Store066Context.Provider value={{}}>{children}</Store066Context.Provider>;
}
