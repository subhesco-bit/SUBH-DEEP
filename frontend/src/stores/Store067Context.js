import { createContext } from 'react';
export const Store067Context = createContext();
export function Store067Provider({ children }) {
  return <Store067Context.Provider value={{}}>{children}</Store067Context.Provider>;
}
