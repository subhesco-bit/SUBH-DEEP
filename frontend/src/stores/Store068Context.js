import { createContext } from 'react';
export const Store068Context = createContext();
export function Store068Provider({ children }) {
  return <Store068Context.Provider value={{}}>{children}</Store068Context.Provider>;
}
