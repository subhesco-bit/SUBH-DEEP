import { createContext } from 'react';
export const Store069Context = createContext();
export function Store069Provider({ children }) {
  return <Store069Context.Provider value={{}}>{children}</Store069Context.Provider>;
}
