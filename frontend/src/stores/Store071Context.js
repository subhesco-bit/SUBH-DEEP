import { createContext } from 'react';
export const Store071Context = createContext();
export function Store071Provider({ children }) {
  return <Store071Context.Provider value={{}}>{children}</Store071Context.Provider>;
}
