import { createContext } from 'react';
export const Store073Context = createContext();
export function Store073Provider({ children }) {
  return <Store073Context.Provider value={{}}>{children}</Store073Context.Provider>;
}
