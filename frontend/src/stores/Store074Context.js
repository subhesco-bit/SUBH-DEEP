import { createContext } from 'react';
export const Store074Context = createContext();
export function Store074Provider({ children }) {
  return <Store074Context.Provider value={{}}>{children}</Store074Context.Provider>;
}
