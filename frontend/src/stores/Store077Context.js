import { createContext } from 'react';
export const Store077Context = createContext();
export function Store077Provider({ children }) {
  return <Store077Context.Provider value={{}}>{children}</Store077Context.Provider>;
}
