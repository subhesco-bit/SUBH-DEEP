import { createContext } from 'react';
export const Store078Context = createContext();
export function Store078Provider({ children }) {
  return <Store078Context.Provider value={{}}>{children}</Store078Context.Provider>;
}
