import { createContext } from 'react';
export const Store079Context = createContext();
export function Store079Provider({ children }) {
  return <Store079Context.Provider value={{}}>{children}</Store079Context.Provider>;
}
