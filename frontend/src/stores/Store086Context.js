import { createContext } from 'react';
export const Store086Context = createContext();
export function Store086Provider({ children }) {
  return <Store086Context.Provider value={{}}>{children}</Store086Context.Provider>;
}
