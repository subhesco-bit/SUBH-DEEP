import { createContext } from 'react';
export const Store087Context = createContext();
export function Store087Provider({ children }) {
  return <Store087Context.Provider value={{}}>{children}</Store087Context.Provider>;
}
