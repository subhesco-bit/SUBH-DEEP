import { createContext } from 'react';
export const Store088Context = createContext();
export function Store088Provider({ children }) {
  return <Store088Context.Provider value={{}}>{children}</Store088Context.Provider>;
}
