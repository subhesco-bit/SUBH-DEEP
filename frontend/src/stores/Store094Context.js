import { createContext } from 'react';
export const Store094Context = createContext();
export function Store094Provider({ children }) {
  return <Store094Context.Provider value={{}}>{children}</Store094Context.Provider>;
}
