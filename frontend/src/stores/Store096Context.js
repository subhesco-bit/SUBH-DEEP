import { createContext } from 'react';
export const Store096Context = createContext();
export function Store096Provider({ children }) {
  return <Store096Context.Provider value={{}}>{children}</Store096Context.Provider>;
}
