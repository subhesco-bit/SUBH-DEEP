import { createContext } from 'react';
export const Store097Context = createContext();
export function Store097Provider({ children }) {
  return <Store097Context.Provider value={{}}>{children}</Store097Context.Provider>;
}
