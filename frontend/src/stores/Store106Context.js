import { createContext } from 'react';
export const Store106Context = createContext();
export function Store106Provider({ children }) {
  return <Store106Context.Provider value={{}}>{children}</Store106Context.Provider>;
}
