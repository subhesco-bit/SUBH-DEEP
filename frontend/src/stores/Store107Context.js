import { createContext } from 'react';
export const Store107Context = createContext();
export function Store107Provider({ children }) {
  return <Store107Context.Provider value={{}}>{children}</Store107Context.Provider>;
}
