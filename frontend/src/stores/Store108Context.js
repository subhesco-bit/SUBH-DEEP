import { createContext } from 'react';
export const Store108Context = createContext();
export function Store108Provider({ children }) {
  return <Store108Context.Provider value={{}}>{children}</Store108Context.Provider>;
}
