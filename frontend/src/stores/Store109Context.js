import { createContext } from 'react';
export const Store109Context = createContext();
export function Store109Provider({ children }) {
  return <Store109Context.Provider value={{}}>{children}</Store109Context.Provider>;
}
