import { createContext } from 'react';
export const Store113Context = createContext();
export function Store113Provider({ children }) {
  return <Store113Context.Provider value={{}}>{children}</Store113Context.Provider>;
}
