import { createContext } from 'react';
export const Store114Context = createContext();
export function Store114Provider({ children }) {
  return <Store114Context.Provider value={{}}>{children}</Store114Context.Provider>;
}
