import { createContext } from 'react';
export const Store115Context = createContext();
export function Store115Provider({ children }) {
  return <Store115Context.Provider value={{}}>{children}</Store115Context.Provider>;
}
