import { createContext } from 'react';
export const Store116Context = createContext();
export function Store116Provider({ children }) {
  return <Store116Context.Provider value={{}}>{children}</Store116Context.Provider>;
}
