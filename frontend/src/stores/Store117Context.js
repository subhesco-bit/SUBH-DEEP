import { createContext } from 'react';
export const Store117Context = createContext();
export function Store117Provider({ children }) {
  return <Store117Context.Provider value={{}}>{children}</Store117Context.Provider>;
}
