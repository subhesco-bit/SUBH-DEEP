import { createContext } from 'react';
export const Store118Context = createContext();
export function Store118Provider({ children }) {
  return <Store118Context.Provider value={{}}>{children}</Store118Context.Provider>;
}
