import { createContext } from 'react';
export const Store119Context = createContext();
export function Store119Provider({ children }) {
  return <Store119Context.Provider value={{}}>{children}</Store119Context.Provider>;
}
