import { createContext } from 'react';
export const Store122Context = createContext();
export function Store122Provider({ children }) {
  return <Store122Context.Provider value={{}}>{children}</Store122Context.Provider>;
}
