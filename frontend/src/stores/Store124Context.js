import { createContext } from 'react';
export const Store124Context = createContext();
export function Store124Provider({ children }) {
  return <Store124Context.Provider value={{}}>{children}</Store124Context.Provider>;
}
