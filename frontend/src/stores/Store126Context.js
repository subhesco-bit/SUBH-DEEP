import { createContext } from 'react';
export const Store126Context = createContext();
export function Store126Provider({ children }) {
  return <Store126Context.Provider value={{}}>{children}</Store126Context.Provider>;
}
