import { createContext } from 'react';
export const Store129Context = createContext();
export function Store129Provider({ children }) {
  return <Store129Context.Provider value={{}}>{children}</Store129Context.Provider>;
}
