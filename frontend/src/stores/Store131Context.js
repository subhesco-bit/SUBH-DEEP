import { createContext } from 'react';
export const Store131Context = createContext();
export function Store131Provider({ children }) {
  return <Store131Context.Provider value={{}}>{children}</Store131Context.Provider>;
}
