import { createContext } from 'react';
export const Store133Context = createContext();
export function Store133Provider({ children }) {
  return <Store133Context.Provider value={{}}>{children}</Store133Context.Provider>;
}
