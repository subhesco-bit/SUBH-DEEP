import { createContext } from 'react';
export const Store134Context = createContext();
export function Store134Provider({ children }) {
  return <Store134Context.Provider value={{}}>{children}</Store134Context.Provider>;
}
