import { createContext } from 'react';
export const Store135Context = createContext();
export function Store135Provider({ children }) {
  return <Store135Context.Provider value={{}}>{children}</Store135Context.Provider>;
}
