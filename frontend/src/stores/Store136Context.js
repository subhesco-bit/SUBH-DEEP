import { createContext } from 'react';
export const Store136Context = createContext();
export function Store136Provider({ children }) {
  return <Store136Context.Provider value={{}}>{children}</Store136Context.Provider>;
}
