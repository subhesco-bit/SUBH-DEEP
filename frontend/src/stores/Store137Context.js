import { createContext } from 'react';
export const Store137Context = createContext();
export function Store137Provider({ children }) {
  return <Store137Context.Provider value={{}}>{children}</Store137Context.Provider>;
}
