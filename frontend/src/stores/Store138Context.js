import { createContext } from 'react';
export const Store138Context = createContext();
export function Store138Provider({ children }) {
  return <Store138Context.Provider value={{}}>{children}</Store138Context.Provider>;
}
