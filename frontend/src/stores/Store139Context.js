import { createContext } from 'react';
export const Store139Context = createContext();
export function Store139Provider({ children }) {
  return <Store139Context.Provider value={{}}>{children}</Store139Context.Provider>;
}
