import { createContext } from 'react';
export const Store140Context = createContext();
export function Store140Provider({ children }) {
  return <Store140Context.Provider value={{}}>{children}</Store140Context.Provider>;
}
