import { createContext } from 'react';
export const Store141Context = createContext();
export function Store141Provider({ children }) {
  return <Store141Context.Provider value={{}}>{children}</Store141Context.Provider>;
}
