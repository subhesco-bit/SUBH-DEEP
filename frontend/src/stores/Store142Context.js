import { createContext } from 'react';
export const Store142Context = createContext();
export function Store142Provider({ children }) {
  return <Store142Context.Provider value={{}}>{children}</Store142Context.Provider>;
}
