import { createContext } from 'react';
export const Store143Context = createContext();
export function Store143Provider({ children }) {
  return <Store143Context.Provider value={{}}>{children}</Store143Context.Provider>;
}
