import { createContext } from 'react';
export const Store144Context = createContext();
export function Store144Provider({ children }) {
  return <Store144Context.Provider value={{}}>{children}</Store144Context.Provider>;
}
