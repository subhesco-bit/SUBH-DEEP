import { createContext } from 'react';
export const Store145Context = createContext();
export function Store145Provider({ children }) {
  return <Store145Context.Provider value={{}}>{children}</Store145Context.Provider>;
}
