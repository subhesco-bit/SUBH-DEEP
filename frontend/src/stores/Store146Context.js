import { createContext } from 'react';
export const Store146Context = createContext();
export function Store146Provider({ children }) {
  return <Store146Context.Provider value={{}}>{children}</Store146Context.Provider>;
}
