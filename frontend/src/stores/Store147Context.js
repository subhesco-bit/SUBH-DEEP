import { createContext } from 'react';
export const Store147Context = createContext();
export function Store147Provider({ children }) {
  return <Store147Context.Provider value={{}}>{children}</Store147Context.Provider>;
}
