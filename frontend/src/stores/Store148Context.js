import { createContext } from 'react';
export const Store148Context = createContext();
export function Store148Provider({ children }) {
  return <Store148Context.Provider value={{}}>{children}</Store148Context.Provider>;
}
