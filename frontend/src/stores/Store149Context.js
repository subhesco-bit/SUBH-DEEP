import { createContext } from 'react';
export const Store149Context = createContext();
export function Store149Provider({ children }) {
  return <Store149Context.Provider value={{}}>{children}</Store149Context.Provider>;
}
