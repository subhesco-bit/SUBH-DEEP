import { createContext } from 'react';
export const Store151Context = createContext();
export function Store151Provider({ children }) {
  return <Store151Context.Provider value={{}}>{children}</Store151Context.Provider>;
}
