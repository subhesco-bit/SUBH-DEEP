import { createContext } from 'react';
export const Store152Context = createContext();
export function Store152Provider({ children }) {
  return <Store152Context.Provider value={{}}>{children}</Store152Context.Provider>;
}
