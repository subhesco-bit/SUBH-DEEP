import { createContext } from 'react';
export const Store153Context = createContext();
export function Store153Provider({ children }) {
  return <Store153Context.Provider value={{}}>{children}</Store153Context.Provider>;
}
