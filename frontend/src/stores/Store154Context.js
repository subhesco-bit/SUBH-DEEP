import { createContext } from 'react';
export const Store154Context = createContext();
export function Store154Provider({ children }) {
  return <Store154Context.Provider value={{}}>{children}</Store154Context.Provider>;
}
