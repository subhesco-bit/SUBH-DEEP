import { createContext } from 'react';
export const Store155Context = createContext();
export function Store155Provider({ children }) {
  return <Store155Context.Provider value={{}}>{children}</Store155Context.Provider>;
}
