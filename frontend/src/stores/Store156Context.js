import { createContext } from 'react';
export const Store156Context = createContext();
export function Store156Provider({ children }) {
  return <Store156Context.Provider value={{}}>{children}</Store156Context.Provider>;
}
