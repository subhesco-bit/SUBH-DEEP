import { createContext } from 'react';
export const Store157Context = createContext();
export function Store157Provider({ children }) {
  return <Store157Context.Provider value={{}}>{children}</Store157Context.Provider>;
}
