import { createContext } from 'react';
export const Store158Context = createContext();
export function Store158Provider({ children }) {
  return <Store158Context.Provider value={{}}>{children}</Store158Context.Provider>;
}
