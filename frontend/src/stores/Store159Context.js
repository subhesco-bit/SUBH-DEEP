import { createContext } from 'react';
export const Store159Context = createContext();
export function Store159Provider({ children }) {
  return <Store159Context.Provider value={{}}>{children}</Store159Context.Provider>;
}
