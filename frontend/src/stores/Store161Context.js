import { createContext } from 'react';
export const Store161Context = createContext();
export function Store161Provider({ children }) {
  return <Store161Context.Provider value={{}}>{children}</Store161Context.Provider>;
}
