import { createContext } from 'react';
export const Store162Context = createContext();
export function Store162Provider({ children }) {
  return <Store162Context.Provider value={{}}>{children}</Store162Context.Provider>;
}
