import { createContext } from 'react';
export const Store164Context = createContext();
export function Store164Provider({ children }) {
  return <Store164Context.Provider value={{}}>{children}</Store164Context.Provider>;
}
