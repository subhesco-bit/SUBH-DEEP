import { createContext } from 'react';
export const Store165Context = createContext();
export function Store165Provider({ children }) {
  return <Store165Context.Provider value={{}}>{children}</Store165Context.Provider>;
}
