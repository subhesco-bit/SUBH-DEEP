import { createContext } from 'react';
export const Store166Context = createContext();
export function Store166Provider({ children }) {
  return <Store166Context.Provider value={{}}>{children}</Store166Context.Provider>;
}
