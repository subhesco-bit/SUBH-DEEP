import { createContext } from 'react';
export const Store167Context = createContext();
export function Store167Provider({ children }) {
  return <Store167Context.Provider value={{}}>{children}</Store167Context.Provider>;
}
