import { createContext } from 'react';
export const Store168Context = createContext();
export function Store168Provider({ children }) {
  return <Store168Context.Provider value={{}}>{children}</Store168Context.Provider>;
}
