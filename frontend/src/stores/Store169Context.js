import { createContext } from 'react';
export const Store169Context = createContext();
export function Store169Provider({ children }) {
  return <Store169Context.Provider value={{}}>{children}</Store169Context.Provider>;
}
