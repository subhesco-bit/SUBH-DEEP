import { createContext } from 'react';
export const Store170Context = createContext();
export function Store170Provider({ children }) {
  return <Store170Context.Provider value={{}}>{children}</Store170Context.Provider>;
}
