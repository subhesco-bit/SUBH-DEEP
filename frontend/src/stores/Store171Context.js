import { createContext } from 'react';
export const Store171Context = createContext();
export function Store171Provider({ children }) {
  return <Store171Context.Provider value={{}}>{children}</Store171Context.Provider>;
}
