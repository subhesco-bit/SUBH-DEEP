import { createContext } from 'react';
export const Store172Context = createContext();
export function Store172Provider({ children }) {
  return <Store172Context.Provider value={{}}>{children}</Store172Context.Provider>;
}
