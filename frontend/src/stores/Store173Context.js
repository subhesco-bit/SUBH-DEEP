import { createContext } from 'react';
export const Store173Context = createContext();
export function Store173Provider({ children }) {
  return <Store173Context.Provider value={{}}>{children}</Store173Context.Provider>;
}
