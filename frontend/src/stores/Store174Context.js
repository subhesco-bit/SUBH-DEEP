import { createContext } from 'react';
export const Store174Context = createContext();
export function Store174Provider({ children }) {
  return <Store174Context.Provider value={{}}>{children}</Store174Context.Provider>;
}
