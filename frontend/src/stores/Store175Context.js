import { createContext } from 'react';
export const Store175Context = createContext();
export function Store175Provider({ children }) {
  return <Store175Context.Provider value={{}}>{children}</Store175Context.Provider>;
}
