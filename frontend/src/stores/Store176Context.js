import { createContext } from 'react';
export const Store176Context = createContext();
export function Store176Provider({ children }) {
  return <Store176Context.Provider value={{}}>{children}</Store176Context.Provider>;
}
