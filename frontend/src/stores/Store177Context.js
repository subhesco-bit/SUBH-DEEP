import { createContext } from 'react';
export const Store177Context = createContext();
export function Store177Provider({ children }) {
  return <Store177Context.Provider value={{}}>{children}</Store177Context.Provider>;
}
