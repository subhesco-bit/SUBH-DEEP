import { createContext } from 'react';
export const Store178Context = createContext();
export function Store178Provider({ children }) {
  return <Store178Context.Provider value={{}}>{children}</Store178Context.Provider>;
}
