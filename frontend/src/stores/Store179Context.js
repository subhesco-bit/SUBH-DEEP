import { createContext } from 'react';
export const Store179Context = createContext();
export function Store179Provider({ children }) {
  return <Store179Context.Provider value={{}}>{children}</Store179Context.Provider>;
}
