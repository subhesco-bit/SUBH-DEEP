import { createContext } from 'react';
export const Store182Context = createContext();
export function Store182Provider({ children }) {
  return <Store182Context.Provider value={{}}>{children}</Store182Context.Provider>;
}
