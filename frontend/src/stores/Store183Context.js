import { createContext } from 'react';
export const Store183Context = createContext();
export function Store183Provider({ children }) {
  return <Store183Context.Provider value={{}}>{children}</Store183Context.Provider>;
}
