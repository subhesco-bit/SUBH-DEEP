import { createContext } from 'react';
export const Store184Context = createContext();
export function Store184Provider({ children }) {
  return <Store184Context.Provider value={{}}>{children}</Store184Context.Provider>;
}
