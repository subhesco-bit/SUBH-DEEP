import { createContext } from 'react';
export const Store185Context = createContext();
export function Store185Provider({ children }) {
  return <Store185Context.Provider value={{}}>{children}</Store185Context.Provider>;
}
