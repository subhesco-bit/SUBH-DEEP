import { createContext } from 'react';
export const Store186Context = createContext();
export function Store186Provider({ children }) {
  return <Store186Context.Provider value={{}}>{children}</Store186Context.Provider>;
}
