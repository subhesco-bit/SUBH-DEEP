import { createContext } from 'react';
export const Store187Context = createContext();
export function Store187Provider({ children }) {
  return <Store187Context.Provider value={{}}>{children}</Store187Context.Provider>;
}
