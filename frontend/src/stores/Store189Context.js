import { createContext } from 'react';
export const Store189Context = createContext();
export function Store189Provider({ children }) {
  return <Store189Context.Provider value={{}}>{children}</Store189Context.Provider>;
}
