import { createContext } from 'react';
export const Store191Context = createContext();
export function Store191Provider({ children }) {
  return <Store191Context.Provider value={{}}>{children}</Store191Context.Provider>;
}
