import { createContext } from 'react';
export const Store193Context = createContext();
export function Store193Provider({ children }) {
  return <Store193Context.Provider value={{}}>{children}</Store193Context.Provider>;
}
