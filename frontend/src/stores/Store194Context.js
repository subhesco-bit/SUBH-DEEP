import { createContext } from 'react';
export const Store194Context = createContext();
export function Store194Provider({ children }) {
  return <Store194Context.Provider value={{}}>{children}</Store194Context.Provider>;
}
