import { createContext } from 'react';
export const Store195Context = createContext();
export function Store195Provider({ children }) {
  return <Store195Context.Provider value={{}}>{children}</Store195Context.Provider>;
}
