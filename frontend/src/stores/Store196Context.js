import { createContext } from 'react';
export const Store196Context = createContext();
export function Store196Provider({ children }) {
  return <Store196Context.Provider value={{}}>{children}</Store196Context.Provider>;
}
