import { createContext } from 'react';
export const Store197Context = createContext();
export function Store197Provider({ children }) {
  return <Store197Context.Provider value={{}}>{children}</Store197Context.Provider>;
}
