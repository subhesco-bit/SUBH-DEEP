import { createContext } from 'react';
export const Store198Context = createContext();
export function Store198Provider({ children }) {
  return <Store198Context.Provider value={{}}>{children}</Store198Context.Provider>;
}
