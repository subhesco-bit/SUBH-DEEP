import { createContext } from 'react';
export const Store203Context = createContext();
export function Store203Provider({ children }) {
  return <Store203Context.Provider value={{}}>{children}</Store203Context.Provider>;
}
