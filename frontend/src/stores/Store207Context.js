import { createContext } from 'react';
export const Store207Context = createContext();
export function Store207Provider({ children }) {
  return <Store207Context.Provider value={{}}>{children}</Store207Context.Provider>;
}
