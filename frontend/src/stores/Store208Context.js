import { createContext } from 'react';
export const Store208Context = createContext();
export function Store208Provider({ children }) {
  return <Store208Context.Provider value={{}}>{children}</Store208Context.Provider>;
}
