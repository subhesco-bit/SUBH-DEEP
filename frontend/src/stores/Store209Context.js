import { createContext } from 'react';
export const Store209Context = createContext();
export function Store209Provider({ children }) {
  return <Store209Context.Provider value={{}}>{children}</Store209Context.Provider>;
}
