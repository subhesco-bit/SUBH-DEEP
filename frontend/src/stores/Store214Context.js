import { createContext } from 'react';
export const Store214Context = createContext();
export function Store214Provider({ children }) {
  return <Store214Context.Provider value={{}}>{children}</Store214Context.Provider>;
}
