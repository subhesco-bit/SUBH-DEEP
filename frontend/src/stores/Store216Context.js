import { createContext } from 'react';
export const Store216Context = createContext();
export function Store216Provider({ children }) {
  return <Store216Context.Provider value={{}}>{children}</Store216Context.Provider>;
}
