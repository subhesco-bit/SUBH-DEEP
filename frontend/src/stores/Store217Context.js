import { createContext } from 'react';
export const Store217Context = createContext();
export function Store217Provider({ children }) {
  return <Store217Context.Provider value={{}}>{children}</Store217Context.Provider>;
}
