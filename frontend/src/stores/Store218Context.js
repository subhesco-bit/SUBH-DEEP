import { createContext } from 'react';
export const Store218Context = createContext();
export function Store218Provider({ children }) {
  return <Store218Context.Provider value={{}}>{children}</Store218Context.Provider>;
}
