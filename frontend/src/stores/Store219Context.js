import { createContext } from 'react';
export const Store219Context = createContext();
export function Store219Provider({ children }) {
  return <Store219Context.Provider value={{}}>{children}</Store219Context.Provider>;
}
