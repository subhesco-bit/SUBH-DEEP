import { createContext } from 'react';
export const Store221Context = createContext();
export function Store221Provider({ children }) {
  return <Store221Context.Provider value={{}}>{children}</Store221Context.Provider>;
}
