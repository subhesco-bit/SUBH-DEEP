import { createContext } from 'react';
export const Store223Context = createContext();
export function Store223Provider({ children }) {
  return <Store223Context.Provider value={{}}>{children}</Store223Context.Provider>;
}
