import { createContext } from 'react';
export const Store224Context = createContext();
export function Store224Provider({ children }) {
  return <Store224Context.Provider value={{}}>{children}</Store224Context.Provider>;
}
