import { createContext } from 'react';
export const Store226Context = createContext();
export function Store226Provider({ children }) {
  return <Store226Context.Provider value={{}}>{children}</Store226Context.Provider>;
}
