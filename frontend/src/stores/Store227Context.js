import { createContext } from 'react';
export const Store227Context = createContext();
export function Store227Provider({ children }) {
  return <Store227Context.Provider value={{}}>{children}</Store227Context.Provider>;
}
