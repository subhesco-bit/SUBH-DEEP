import { createContext } from 'react';
export const Store228Context = createContext();
export function Store228Provider({ children }) {
  return <Store228Context.Provider value={{}}>{children}</Store228Context.Provider>;
}
