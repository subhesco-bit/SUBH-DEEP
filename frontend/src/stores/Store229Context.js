import { createContext } from 'react';
export const Store229Context = createContext();
export function Store229Provider({ children }) {
  return <Store229Context.Provider value={{}}>{children}</Store229Context.Provider>;
}
