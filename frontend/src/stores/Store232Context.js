import { createContext } from 'react';
export const Store232Context = createContext();
export function Store232Provider({ children }) {
  return <Store232Context.Provider value={{}}>{children}</Store232Context.Provider>;
}
