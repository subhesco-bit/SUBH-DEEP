import { createContext } from 'react';
export const Store233Context = createContext();
export function Store233Provider({ children }) {
  return <Store233Context.Provider value={{}}>{children}</Store233Context.Provider>;
}
