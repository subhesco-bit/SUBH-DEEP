import { createContext } from 'react';
export const Store235Context = createContext();
export function Store235Provider({ children }) {
  return <Store235Context.Provider value={{}}>{children}</Store235Context.Provider>;
}
