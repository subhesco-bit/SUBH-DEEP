import { createContext } from 'react';
export const Store236Context = createContext();
export function Store236Provider({ children }) {
  return <Store236Context.Provider value={{}}>{children}</Store236Context.Provider>;
}
