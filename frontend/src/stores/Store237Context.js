import { createContext } from 'react';
export const Store237Context = createContext();
export function Store237Provider({ children }) {
  return <Store237Context.Provider value={{}}>{children}</Store237Context.Provider>;
}
