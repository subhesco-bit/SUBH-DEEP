import { createContext } from 'react';
export const Store238Context = createContext();
export function Store238Provider({ children }) {
  return <Store238Context.Provider value={{}}>{children}</Store238Context.Provider>;
}
