import { createContext } from 'react';
export const Store239Context = createContext();
export function Store239Provider({ children }) {
  return <Store239Context.Provider value={{}}>{children}</Store239Context.Provider>;
}
