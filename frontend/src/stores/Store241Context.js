import { createContext } from 'react';
export const Store241Context = createContext();
export function Store241Provider({ children }) {
  return <Store241Context.Provider value={{}}>{children}</Store241Context.Provider>;
}
