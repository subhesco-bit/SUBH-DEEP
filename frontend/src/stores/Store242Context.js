import { createContext } from 'react';
export const Store242Context = createContext();
export function Store242Provider({ children }) {
  return <Store242Context.Provider value={{}}>{children}</Store242Context.Provider>;
}
