import { createContext } from 'react';
export const Store243Context = createContext();
export function Store243Provider({ children }) {
  return <Store243Context.Provider value={{}}>{children}</Store243Context.Provider>;
}
