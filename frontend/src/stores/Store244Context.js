import { createContext } from 'react';
export const Store244Context = createContext();
export function Store244Provider({ children }) {
  return <Store244Context.Provider value={{}}>{children}</Store244Context.Provider>;
}
