import { createContext } from 'react';
export const Store245Context = createContext();
export function Store245Provider({ children }) {
  return <Store245Context.Provider value={{}}>{children}</Store245Context.Provider>;
}
