import { createContext } from 'react';
export const Store246Context = createContext();
export function Store246Provider({ children }) {
  return <Store246Context.Provider value={{}}>{children}</Store246Context.Provider>;
}
