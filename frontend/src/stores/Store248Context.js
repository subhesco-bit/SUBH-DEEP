import { createContext } from 'react';
export const Store248Context = createContext();
export function Store248Provider({ children }) {
  return <Store248Context.Provider value={{}}>{children}</Store248Context.Provider>;
}
