import { createContext } from 'react';
export const Store249Context = createContext();
export function Store249Provider({ children }) {
  return <Store249Context.Provider value={{}}>{children}</Store249Context.Provider>;
}
