import { createContext } from 'react';
export const Store252Context = createContext();
export function Store252Provider({ children }) {
  return <Store252Context.Provider value={{}}>{children}</Store252Context.Provider>;
}
