import { createContext } from 'react';
export const Store253Context = createContext();
export function Store253Provider({ children }) {
  return <Store253Context.Provider value={{}}>{children}</Store253Context.Provider>;
}
