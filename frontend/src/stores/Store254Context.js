import { createContext } from 'react';
export const Store254Context = createContext();
export function Store254Provider({ children }) {
  return <Store254Context.Provider value={{}}>{children}</Store254Context.Provider>;
}
