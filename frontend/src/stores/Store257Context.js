import { createContext } from 'react';
export const Store257Context = createContext();
export function Store257Provider({ children }) {
  return <Store257Context.Provider value={{}}>{children}</Store257Context.Provider>;
}
