import { createContext } from 'react';
export const Store258Context = createContext();
export function Store258Provider({ children }) {
  return <Store258Context.Provider value={{}}>{children}</Store258Context.Provider>;
}
