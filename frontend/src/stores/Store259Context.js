import { createContext } from 'react';
export const Store259Context = createContext();
export function Store259Provider({ children }) {
  return <Store259Context.Provider value={{}}>{children}</Store259Context.Provider>;
}
