import { createContext } from 'react';
export const Store261Context = createContext();
export function Store261Provider({ children }) {
  return <Store261Context.Provider value={{}}>{children}</Store261Context.Provider>;
}
