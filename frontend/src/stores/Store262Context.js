import { createContext } from 'react';
export const Store262Context = createContext();
export function Store262Provider({ children }) {
  return <Store262Context.Provider value={{}}>{children}</Store262Context.Provider>;
}
