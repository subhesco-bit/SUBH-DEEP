import { createContext } from 'react';
export const Store263Context = createContext();
export function Store263Provider({ children }) {
  return <Store263Context.Provider value={{}}>{children}</Store263Context.Provider>;
}
