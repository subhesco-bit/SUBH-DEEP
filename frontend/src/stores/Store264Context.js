import { createContext } from 'react';
export const Store264Context = createContext();
export function Store264Provider({ children }) {
  return <Store264Context.Provider value={{}}>{children}</Store264Context.Provider>;
}
