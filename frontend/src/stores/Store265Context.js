import { createContext } from 'react';
export const Store265Context = createContext();
export function Store265Provider({ children }) {
  return <Store265Context.Provider value={{}}>{children}</Store265Context.Provider>;
}
