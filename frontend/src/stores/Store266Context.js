import { createContext } from 'react';
export const Store266Context = createContext();
export function Store266Provider({ children }) {
  return <Store266Context.Provider value={{}}>{children}</Store266Context.Provider>;
}
