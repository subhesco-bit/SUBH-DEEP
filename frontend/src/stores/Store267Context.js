import { createContext } from 'react';
export const Store267Context = createContext();
export function Store267Provider({ children }) {
  return <Store267Context.Provider value={{}}>{children}</Store267Context.Provider>;
}
