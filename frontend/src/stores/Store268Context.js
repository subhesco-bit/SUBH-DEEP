import { createContext } from 'react';
export const Store268Context = createContext();
export function Store268Provider({ children }) {
  return <Store268Context.Provider value={{}}>{children}</Store268Context.Provider>;
}
