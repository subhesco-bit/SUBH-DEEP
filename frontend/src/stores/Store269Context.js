import { createContext } from 'react';
export const Store269Context = createContext();
export function Store269Provider({ children }) {
  return <Store269Context.Provider value={{}}>{children}</Store269Context.Provider>;
}
