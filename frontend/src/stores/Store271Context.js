import { createContext } from 'react';
export const Store271Context = createContext();
export function Store271Provider({ children }) {
  return <Store271Context.Provider value={{}}>{children}</Store271Context.Provider>;
}
