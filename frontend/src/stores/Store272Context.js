import { createContext } from 'react';
export const Store272Context = createContext();
export function Store272Provider({ children }) {
  return <Store272Context.Provider value={{}}>{children}</Store272Context.Provider>;
}
