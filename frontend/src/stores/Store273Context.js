import { createContext } from 'react';
export const Store273Context = createContext();
export function Store273Provider({ children }) {
  return <Store273Context.Provider value={{}}>{children}</Store273Context.Provider>;
}
