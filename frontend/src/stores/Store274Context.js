import { createContext } from 'react';
export const Store274Context = createContext();
export function Store274Provider({ children }) {
  return <Store274Context.Provider value={{}}>{children}</Store274Context.Provider>;
}
