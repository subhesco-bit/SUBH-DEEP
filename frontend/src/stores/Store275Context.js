import { createContext } from 'react';
export const Store275Context = createContext();
export function Store275Provider({ children }) {
  return <Store275Context.Provider value={{}}>{children}</Store275Context.Provider>;
}
