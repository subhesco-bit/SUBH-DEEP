import { createContext } from 'react';
export const Store276Context = createContext();
export function Store276Provider({ children }) {
  return <Store276Context.Provider value={{}}>{children}</Store276Context.Provider>;
}
