import { createContext } from 'react';
export const Store277Context = createContext();
export function Store277Provider({ children }) {
  return <Store277Context.Provider value={{}}>{children}</Store277Context.Provider>;
}
