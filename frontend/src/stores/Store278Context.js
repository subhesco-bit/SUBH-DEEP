import { createContext } from 'react';
export const Store278Context = createContext();
export function Store278Provider({ children }) {
  return <Store278Context.Provider value={{}}>{children}</Store278Context.Provider>;
}
