import { createContext } from 'react';
export const Store279Context = createContext();
export function Store279Provider({ children }) {
  return <Store279Context.Provider value={{}}>{children}</Store279Context.Provider>;
}
