import { createContext } from 'react';
export const Store282Context = createContext();
export function Store282Provider({ children }) {
  return <Store282Context.Provider value={{}}>{children}</Store282Context.Provider>;
}
