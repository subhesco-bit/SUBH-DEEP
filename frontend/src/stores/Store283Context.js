import { createContext } from 'react';
export const Store283Context = createContext();
export function Store283Provider({ children }) {
  return <Store283Context.Provider value={{}}>{children}</Store283Context.Provider>;
}
