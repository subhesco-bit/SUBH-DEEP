import { createContext } from 'react';
export const Store284Context = createContext();
export function Store284Provider({ children }) {
  return <Store284Context.Provider value={{}}>{children}</Store284Context.Provider>;
}
