import { createContext } from 'react';
export const Store285Context = createContext();
export function Store285Provider({ children }) {
  return <Store285Context.Provider value={{}}>{children}</Store285Context.Provider>;
}
