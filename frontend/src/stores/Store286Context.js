import { createContext } from 'react';
export const Store286Context = createContext();
export function Store286Provider({ children }) {
  return <Store286Context.Provider value={{}}>{children}</Store286Context.Provider>;
}
