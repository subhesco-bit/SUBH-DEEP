import { createContext } from 'react';
export const Store287Context = createContext();
export function Store287Provider({ children }) {
  return <Store287Context.Provider value={{}}>{children}</Store287Context.Provider>;
}
