import { createContext } from 'react';
export const Store288Context = createContext();
export function Store288Provider({ children }) {
  return <Store288Context.Provider value={{}}>{children}</Store288Context.Provider>;
}
