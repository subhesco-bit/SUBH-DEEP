import { createContext } from 'react';
export const Store289Context = createContext();
export function Store289Provider({ children }) {
  return <Store289Context.Provider value={{}}>{children}</Store289Context.Provider>;
}
