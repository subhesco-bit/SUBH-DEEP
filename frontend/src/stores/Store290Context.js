import { createContext } from 'react';
export const Store290Context = createContext();
export function Store290Provider({ children }) {
  return <Store290Context.Provider value={{}}>{children}</Store290Context.Provider>;
}
