import { createContext } from 'react';
export const Store291Context = createContext();
export function Store291Provider({ children }) {
  return <Store291Context.Provider value={{}}>{children}</Store291Context.Provider>;
}
