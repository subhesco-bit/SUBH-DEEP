import { createContext } from 'react';
export const Store292Context = createContext();
export function Store292Provider({ children }) {
  return <Store292Context.Provider value={{}}>{children}</Store292Context.Provider>;
}
