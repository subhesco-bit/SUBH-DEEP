import { createContext } from 'react';
export const Store293Context = createContext();
export function Store293Provider({ children }) {
  return <Store293Context.Provider value={{}}>{children}</Store293Context.Provider>;
}
