import { createContext } from 'react';
export const Store294Context = createContext();
export function Store294Provider({ children }) {
  return <Store294Context.Provider value={{}}>{children}</Store294Context.Provider>;
}
