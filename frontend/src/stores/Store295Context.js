import { createContext } from 'react';
export const Store295Context = createContext();
export function Store295Provider({ children }) {
  return <Store295Context.Provider value={{}}>{children}</Store295Context.Provider>;
}
