import { createContext } from 'react';
export const Store296Context = createContext();
export function Store296Provider({ children }) {
  return <Store296Context.Provider value={{}}>{children}</Store296Context.Provider>;
}
