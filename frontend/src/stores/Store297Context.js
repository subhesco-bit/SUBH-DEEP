import { createContext } from 'react';
export const Store297Context = createContext();
export function Store297Provider({ children }) {
  return <Store297Context.Provider value={{}}>{children}</Store297Context.Provider>;
}
