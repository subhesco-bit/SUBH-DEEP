import { createContext } from 'react';
export const Store298Context = createContext();
export function Store298Provider({ children }) {
  return <Store298Context.Provider value={{}}>{children}</Store298Context.Provider>;
}
