import { createContext } from 'react';
export const Store299Context = createContext();
export function Store299Provider({ children }) {
  return <Store299Context.Provider value={{}}>{children}</Store299Context.Provider>;
}
