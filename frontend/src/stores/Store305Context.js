import { createContext } from 'react';
export const Store305Context = createContext();
export function Store305Provider({ children }) {
  return <Store305Context.Provider value={{}}>{children}</Store305Context.Provider>;
}
