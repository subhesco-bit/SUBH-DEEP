import { createContext } from 'react';
export const Store306Context = createContext();
export function Store306Provider({ children }) {
  return <Store306Context.Provider value={{}}>{children}</Store306Context.Provider>;
}
