import { createContext } from 'react';
export const Store307Context = createContext();
export function Store307Provider({ children }) {
  return <Store307Context.Provider value={{}}>{children}</Store307Context.Provider>;
}
