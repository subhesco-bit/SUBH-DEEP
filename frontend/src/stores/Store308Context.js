import { createContext } from 'react';
export const Store308Context = createContext();
export function Store308Provider({ children }) {
  return <Store308Context.Provider value={{}}>{children}</Store308Context.Provider>;
}
