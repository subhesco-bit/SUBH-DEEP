import { createContext } from 'react';
export const Store309Context = createContext();
export function Store309Provider({ children }) {
  return <Store309Context.Provider value={{}}>{children}</Store309Context.Provider>;
}
