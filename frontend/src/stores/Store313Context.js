import { createContext } from 'react';
export const Store313Context = createContext();
export function Store313Provider({ children }) {
  return <Store313Context.Provider value={{}}>{children}</Store313Context.Provider>;
}
