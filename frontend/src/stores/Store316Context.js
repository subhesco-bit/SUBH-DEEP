import { createContext } from 'react';
export const Store316Context = createContext();
export function Store316Provider({ children }) {
  return <Store316Context.Provider value={{}}>{children}</Store316Context.Provider>;
}
