import { createContext } from 'react';
export const Store317Context = createContext();
export function Store317Provider({ children }) {
  return <Store317Context.Provider value={{}}>{children}</Store317Context.Provider>;
}
