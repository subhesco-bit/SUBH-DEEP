import { createContext } from 'react';
export const Store318Context = createContext();
export function Store318Provider({ children }) {
  return <Store318Context.Provider value={{}}>{children}</Store318Context.Provider>;
}
