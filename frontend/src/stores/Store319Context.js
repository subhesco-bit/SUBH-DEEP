import { createContext } from 'react';
export const Store319Context = createContext();
export function Store319Provider({ children }) {
  return <Store319Context.Provider value={{}}>{children}</Store319Context.Provider>;
}
