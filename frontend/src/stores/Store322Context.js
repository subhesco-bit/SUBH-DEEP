import { createContext } from 'react';
export const Store322Context = createContext();
export function Store322Provider({ children }) {
  return <Store322Context.Provider value={{}}>{children}</Store322Context.Provider>;
}
