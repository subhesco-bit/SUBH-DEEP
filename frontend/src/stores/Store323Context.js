import { createContext } from 'react';
export const Store323Context = createContext();
export function Store323Provider({ children }) {
  return <Store323Context.Provider value={{}}>{children}</Store323Context.Provider>;
}
