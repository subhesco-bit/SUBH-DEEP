import { createContext } from 'react';
export const Store324Context = createContext();
export function Store324Provider({ children }) {
  return <Store324Context.Provider value={{}}>{children}</Store324Context.Provider>;
}
