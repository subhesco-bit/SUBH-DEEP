import { createContext } from 'react';
export const Store326Context = createContext();
export function Store326Provider({ children }) {
  return <Store326Context.Provider value={{}}>{children}</Store326Context.Provider>;
}
