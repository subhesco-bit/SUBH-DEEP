import { createContext } from 'react';
export const Store327Context = createContext();
export function Store327Provider({ children }) {
  return <Store327Context.Provider value={{}}>{children}</Store327Context.Provider>;
}
