import { createContext } from 'react';
export const Store328Context = createContext();
export function Store328Provider({ children }) {
  return <Store328Context.Provider value={{}}>{children}</Store328Context.Provider>;
}
