import { createContext } from 'react';
export const Store329Context = createContext();
export function Store329Provider({ children }) {
  return <Store329Context.Provider value={{}}>{children}</Store329Context.Provider>;
}
