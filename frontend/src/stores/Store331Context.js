import { createContext } from 'react';
export const Store331Context = createContext();
export function Store331Provider({ children }) {
  return <Store331Context.Provider value={{}}>{children}</Store331Context.Provider>;
}
