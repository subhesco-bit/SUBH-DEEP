import { createContext } from 'react';
export const Store332Context = createContext();
export function Store332Provider({ children }) {
  return <Store332Context.Provider value={{}}>{children}</Store332Context.Provider>;
}
