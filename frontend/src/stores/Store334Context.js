import { createContext } from 'react';
export const Store334Context = createContext();
export function Store334Provider({ children }) {
  return <Store334Context.Provider value={{}}>{children}</Store334Context.Provider>;
}
