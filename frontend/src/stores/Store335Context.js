import { createContext } from 'react';
export const Store335Context = createContext();
export function Store335Provider({ children }) {
  return <Store335Context.Provider value={{}}>{children}</Store335Context.Provider>;
}
