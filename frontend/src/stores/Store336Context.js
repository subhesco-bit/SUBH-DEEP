import { createContext } from 'react';
export const Store336Context = createContext();
export function Store336Provider({ children }) {
  return <Store336Context.Provider value={{}}>{children}</Store336Context.Provider>;
}
