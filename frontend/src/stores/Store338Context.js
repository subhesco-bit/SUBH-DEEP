import { createContext } from 'react';
export const Store338Context = createContext();
export function Store338Provider({ children }) {
  return <Store338Context.Provider value={{}}>{children}</Store338Context.Provider>;
}
