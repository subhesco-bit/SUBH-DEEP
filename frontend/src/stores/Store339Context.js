import { createContext } from 'react';
export const Store339Context = createContext();
export function Store339Provider({ children }) {
  return <Store339Context.Provider value={{}}>{children}</Store339Context.Provider>;
}
