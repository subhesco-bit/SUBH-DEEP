import { createContext } from 'react';
export const Store340Context = createContext();
export function Store340Provider({ children }) {
  return <Store340Context.Provider value={{}}>{children}</Store340Context.Provider>;
}
