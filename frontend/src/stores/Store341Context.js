import { createContext } from 'react';
export const Store341Context = createContext();
export function Store341Provider({ children }) {
  return <Store341Context.Provider value={{}}>{children}</Store341Context.Provider>;
}
