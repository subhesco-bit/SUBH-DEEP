import { createContext } from 'react';
export const Store342Context = createContext();
export function Store342Provider({ children }) {
  return <Store342Context.Provider value={{}}>{children}</Store342Context.Provider>;
}
